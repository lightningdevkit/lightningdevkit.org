import{c as t,Q as a,j as i,m as n}from"./chunks/framework.BPKcPtvA.js";const o="/assets/gatewayd-arch.DOIWgKJ6.png",s="/assets/gatewayd-arch-ldk2.BNG-nr2F.png",y=JSON.parse('{"title":"Fedimint Lightning Gateway uses LDK Node to simplify deployment and liquidity management","description":"Learn how Fedimint uses LDK node to simplify deployment and liquidity management in the lightning gateway","frontmatter":{"title":"Fedimint Lightning Gateway uses LDK Node to simplify deployment and liquidity management","description":"Learn how Fedimint uses LDK node to simplify deployment and liquidity management in the lightning gateway","date":"2025-01-30","authors":["Justin Moeller"],"tags":["case-studies"]},"headers":[],"relativePath":"blog/fedimint-lightning-gateway-uses-ldk-node-to-simplify-deployment-and-liquidity-management.md","filePath":"blog/fedimint-lightning-gateway-uses-ldk-node-to-simplify-deployment-and-liquidity-management.md","lastUpdated":1782480285000}'),d={name:"blog/fedimint-lightning-gateway-uses-ldk-node-to-simplify-deployment-and-liquidity-management.md"};function r(h,e,l,g,m,c){return a(),i("div",null,[...e[0]||(e[0]=[n('<p>The <a href="https://github.com/fedimint/fedimint/blob/master/docs/gateway.md" target="_blank" rel="noreferrer">Fedimint Lightning Gateway</a> is a server application that powers the Lightning payments for Fedimints. Lightning Gateways enable Fedimint ecash users access to the broader Lightning network by offering a trustless ecash to Lightning swap service to the federation. A Fedimint user incentivizes the gateway to pay a Lightning invoice on its behalf by locking ecash to a hash time locked contract that is enforced by the federation. The gateway can then claim the locked ecash by revealing the preimage from the paid invoice. To receive ecash, the gateway will fund a Fedimint client as long as it receives a preimage in return that the gateway can use to claim a payment on the Lightning network. If this sounds similar to how HTLCs in Lightning work, that&#39;s because it is! However, there is the added caveat that Lightning Gateways must also manage an ecash balance alongside the Lightning channels.</p><p>Lightning Gateways can also be connected to more than one federation. This allows the gateway to provide Lightning services to many Fedimints at once and also short-circuit the Lightning network altogether if the payment is between connected federations (or within the same federation). The Lightning Gateway must hold a balance for each federation it services payments for, so that it can fund incoming payments. Conceptually, this is similar to a &quot;virtual channel&quot; between the Lightning Gateway and the target federation. The ecash balance that the gateway holds for each federation is analogous to inbound liquidity in Lightning.</p><p><img src="'+o+'" alt="Fedimint Lightning Gateway"></p><p>Previously, gateway operators had the option of connecting their gateway to either a Core Lightning or LND node. While this deployment configuration is convenient for node runners who have existing liquidity, this comes with additional technical burden and operational difficulties for new gateways. Gateway operators need to deploy and operate two separate daemons (e.g <code>LND</code> and <code>gatewayd</code>) and monitor liquidity for their channels and connected federations in each. It is our goal to make it as easy as possible to use Fedimint, so to reduce these operational complexities, it made sense to integrate LDK node directly into the Lightning Gateway to provide a seamless ecash and Lightning experience.</p><h1 id="what-we-did" tabindex="-1">What we did <a class="header-anchor" href="#what-we-did" aria-label="Permalink to &quot;What we did&quot;">​</a></h1><p>We chose to use LDK Node because of the ease of use for getting started. LDK Node has numerous pre-set defaults that make deploying a Lightning node easy, such as an integrated BDK wallet and a SQLite database. Because we already supported LND and CLN, we had done work previously to abstract away the interface to the Lightning node. This made the implementation of LDK Node into the Lightning Gateway straightforward - only a handful of functions needed to be implemented.</p><p>However, in order for the Lightning Gateway to receive a Lightning payment on behalf of a Fedimint user in a trustless manner, we needed the ability to create invoices where LDK Node does not know the preimage (i.e a HOLD invoice). This was not originally supported in LDK Node. LDK Node intentionally has a slim API, but after discussing our use case with the LDK team, the API was exposed.</p><p><img src="'+s+'" alt="LDK Node Lightning Gateway"></p><p>Integrating LDK Node into the Lightning Gateway also exposed new challenges. Previously, for Lightning management operations such as opening and closing channels, we relied on the operator interacting directly with the underlying Lightning node. With LDK Node integrated into the gateway, the gateway operator no longer has another daemon to fall back to for channel or UTXO management; it must be done within the gateway itself. Most of the work we did was simply exposing Lightning operations and debugging logs to the gateway operator.</p><p>With ecash and Lightning management available in a single daemon, we are looking to explore automated methods of managing liquidity, such as the <a href="https://github.com/lightning/blips/blob/master/blip-0051.md" target="_blank" rel="noreferrer">LSPv1 spec</a>. We believe that ecash presents an opportunity to make liquidity management more efficient and are hoping to build this directly into the gateway itself.</p><h1 id="results" tabindex="-1">Results <a class="header-anchor" href="#results" aria-label="Permalink to &quot;Results&quot;">​</a></h1><p>The LDK Gateway will be available to use as a Lightning node in the v0.5 Fedimint release and will be able to service new federations starting with v0.6. We hope to see new node operators deploy <code>gatewayd</code> and take advantage of the integrated Lightning node for simpler deployment and liquidity management. Feedback is always welcome, <a href="https://discord.gg/cEVEmqCgWG" target="_blank" rel="noreferrer">please join our discord channel</a> to get in touch with the developers or submit an issue on our <a href="https://github.com/fedimint/fedimint" target="_blank" rel="noreferrer">Github</a>.</p><p>Shoutout to tnull, tvolk131, joschican, and elsirion for reviewing drafts of this document and their work on the LDK Gateway.</p>',13)])])}const u=t(d,[["render",r]]);export{y as __pageData,u as default};
